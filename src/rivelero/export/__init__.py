"""Scientific data export (P2).

Machine-readable interoperability products - GeoTIFF rasters and CSV tables
with JSON metadata sidecars - for GIS, Python/R, spreadsheets and archival
workflows. The ``.rivelero`` project file (rivelero.project) remains the
authoritative persistence format; exports are copies of results, not a
project.

The package is independent of Qt.
"""
