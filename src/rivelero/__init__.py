"""Rivelero: spatial observability of field surveys.

The scientific packages (core, observability, visibility, analysis, export,
project, io) import without Qt or a display. The graphical application lives
in ``rivelero.gui`` and is started with ``rivelero`` / ``python -m rivelero``.
"""

# Single source of the package version (read by pyproject.toml and recorded
# in saved projects, exports and provenance). Development versions only
# until a first public release is decided.
__version__ = "0.1.0.dev0"
