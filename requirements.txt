# Prefer `conda env create -f environment.yml` and then `pip install -e ".[gui,test]"`.
# GDAL's Python bindings (osgeo) are also required for viewsheds; install them
# for your platform (conda-forge recommended) - they are not listed here.

# Core scientific stack used by the current application
numpy
rasterio
affine
pyproj
shapely
matplotlib
requests

# Graphical application
PySide6

# Tests
pytest
pillow

# Used only by legacy modules; not needed by the current application
geopandas
osmnx
OpenEXR
psutil
python-dateutil

# Not imported anywhere in the repository (candidates for removal)
pandas
scikit-learn
lark
